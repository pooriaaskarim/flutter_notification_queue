name: in_app_notifications
description: "An overlay based In App Notification"
version: 0.0.1
homepage: https://github.com/pooriaaskarim/in_app_notification
repository: https://github.com/pooriaaskarim/in_app_notification

environment:
  sdk: '>=2.18.0 <=3.9.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true
