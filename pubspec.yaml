name: in_app_notifications
description: "An overlay based In App Notification"
version: 0.2.0
homepage: https://github.com/pooriaaskarim/in_app_notifications
repository: https://github.com/pooriaaskarim/in_app_notifications
publish_to: https://pub.dev/pooriaaskarim/in_app_notification
topics: [flutter, notification, overlay, toast, popup]
license: MIT

environment:
  sdk: '>=2.18.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true
